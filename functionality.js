window.addEventListener("DOMContentLoaded", domLoaded);
function domLoaded () {

    document.querySelector("#custom-submit-button").addEventListener("click", submitEventHandler);
}

submitEventHandler(); {
    fileupload.on('change', function(){}); // Gets user input file
}